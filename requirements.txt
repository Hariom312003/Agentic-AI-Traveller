# ── Core orchestration ────────────────────────────────────────────────
# langchain-core is intentionally NOT pinned here: it's a transitive
# dependency of langgraph, which requires a specific compatible range that
# changes between langgraph releases. Pinning our own version here caused a
# real ResolutionImpossible conflict during testing — let pip resolve it.
langgraph==1.2.9
langgraph-checkpoint-sqlite==3.1.0

# ── API layer ─────────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.30.6
python-multipart==0.0.12

# ── Data / validation ─────────────────────────────────────────────────
pydantic==2.9.2
pydantic-settings==2.5.2

# ── RAG stack ─────────────────────────────────────────────────────────
chromadb==1.5.9
rank-bm25==0.2.2
scikit-learn==1.5.2
numpy>=1.26,<3.0

# ── Validation helpers ────────────────────────────────────────────────
rapidfuzz==3.9.7

# ── LLM provider SDKs ─────────────────────────────────────────────────
google-genai==1.20.0
groq==0.11.0
openai==1.45.0
anthropic==0.40.0
# tenacity: ranged, not exact-pinned — chromadb requires >=8.2.3 while
# streamlit requires <9; ==9.0.0 satisfies neither pair simultaneously.
tenacity>=8.2.3,<9.0.0

# ── Frontend ──────────────────────────────────────────────────────────
streamlit==1.38.0
plotly==5.24.1
pandas==2.2.3
fpdf2==2.8.1
folium==0.17.0
geopy==2.4.1

# ── Utilities ─────────────────────────────────────────────────────────
python-dotenv==1.0.1
# httpx: NOT an exact pin — chromadb requires >=0.27.0 while google-genai
# requires >=0.28.1; a hard ==0.27.2 pin (what a naive first pass at this
# file had) makes those unsatisfiable together. Let pip resolve within a
# range both are happy with.
httpx>=0.28.1,<1.0.0
requests==2.32.3
rich==13.9.2

# ── Testing ───────────────────────────────────────────────────────────
pytest==8.3.3
pytest-mock==3.14.0
pytest-cov==5.0.0
